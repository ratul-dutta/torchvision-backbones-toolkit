# torchvision-backbones-toolkit
PyTorch utilities for pretrained VGG19, ResNet18, and ResNet34: ImageNet-1k inference, custom classification heads for transfer learning, and embedding extractors for downstream tasks like retrieval and captioning.
